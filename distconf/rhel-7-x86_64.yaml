extends: "lib/rhel.yaml"

os:
  version: 7
  arch: x86_64

macros:
  libdir: $prefix/lib64
  # systemd stuff
  unitdir: /usr/lib/systemd/system
  userunitdir: /usr/lib/systemd/user

docker:
  from: rhel7
